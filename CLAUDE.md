# 병원 프리미엄 웹사이트 프로젝트 핵심 규칙

## 사용자와 설명 방식

- 사용자는 코드 초보자일 수 있다.
- 작업 지시는 `파일명 / 작업방식 / 수정 이유 / 영향 범위 / 검증 명령` 순서로 설명한다.
- 오류 발생 시 첫 줄에 파일명·줄번호·원인을 한 문장으로 말한다.
- 영어 오류 메시지를 한국어로 해석하고 최소 수정 방법을 제시한다.
- 불확실한 내용은 추측으로 수정하지 말고 `확인 필요`로 표시한다.

## 작업 시작 전

- 먼저 코드베이스, 현재 URL, 기존 검색 성과, 운영 환경을 분석한다.
- 신규 구축인지, 구버전 유지인지, 부분 최적화인지, 1:1 마이그레이션인지 판정한다.
- 실제 코드 확인 전 파일 구조·URL·schema를 추측하지 않는다.
- 기존 검색 상위노출 페이지는 보존이 우선이다.
- 검색 성과 데이터 없이 title·H1·URL·canonical·본문을 대량 변경하지 않는다.

## 디자인과 UX

- 기능이 동작하는 것만으로 완료하지 않는다.
- PC와 모바일을 독립적으로 설계하고 검수한다.
- 페이지마다 임의 spacing을 사용하지 말고 디자인 토큰을 사용한다.
- 모든 섹션은 목적, 핵심 메시지, CTA, 모바일 배치, 성능 제한을 가진다.
- 프리미엄 디자인은 과도한 효과가 아니라 정렬, 여백, 타이포, 이미지 톤, 정보 계층의 일관성으로 만든다.
- 영상·모션은 전환과 이해에 도움이 될 때만 사용하고 reduced-motion을 지원한다.
- 모바일 가로 스크롤, 겹침, 잘린 CTA, 키보드 가림을 허용하지 않는다.

## 개발

- Next.js App Router, TypeScript strict, Tailwind CSS를 기본으로 한다.
- Server Component를 우선하고 Client Component는 상호작용이 필요한 범위로 제한한다.
- semantic HTML, H1 1개, 올바른 heading 계층, 접근 가능한 폼을 사용한다.
- Next/Image에 width·height 또는 fill, sizes, alt를 제공한다.
- 환경변수는 서버·클라이언트 범위를 구분하고 runtime validation을 적용한다.
- 오류·로딩·빈 상태·404를 구현한다.
- 변경 후 lint, typecheck, test, build를 실행한다.

## 백엔드·DB·보안

- 운영 DB 변경, RLS 변경, migration 적용은 사람 승인을 요구한다.
- Service Role Key를 클라이언트에 노출하지 않는다.
- 개인정보·건강 관련 정보는 최소 수집하고 접근 권한과 감사 로그를 설계한다.
- 폼 입력 검증, rate limit, bot 방지, 중복 문의 방지를 적용한다.
- 관리자·상담원·의료진·대행사 권한을 분리한다.
- 개인정보 수집 동의와 광고성 정보 수신 동의를 분리한다.

## SEO·AEO·GEO·LLMEO

- 색인과 순위를 구분한다.
- 페이지별 대표 검색 의도와 대표 URL을 하나로 정한다.
- title·description·canonical·Open Graph·schema URL을 일치시킨다.
- 모든 페이지에 FAQPage·HowTo를 기계적으로 넣지 않는다.
- 실제 화면에 없는 내용을 JSON-LD에 넣지 않는다.
- 의료 콘텐츠는 작성자, 검수자, 검수일, 공식 출처를 관리한다.
- 네이버, Google, Bing을 동일하게 취급하지 않는다.
- 기존 네이버 노출이 좋은 경우 최소 수정 원칙을 따른다.

## Git·배포

- 작업 전 `git status`와 현재 branch를 확인한다.
- 사용자 미커밋 변경을 덮어쓰지 않는다.
- `git reset --hard`, 강제 push, stash drop은 자동 실행하지 않는다.
- Production 배포, DNS, 환경변수, 외부 메시지 발송은 사람 승인을 요구한다.
- Preview에서 검수 후 Production으로 이동한다.
- 배포 전후 변경 목록, 검증 결과, 롤백 방법을 기록한다.

## 완료 보고 형식

1. 수정 파일
2. 수정 이유
3. 사용자 화면 변화
4. SEO·DB·보안 영향
5. 실행한 검증
6. 실패 또는 미검증 항목
7. 롤백 방법
8. 다음 권장 작업
